# Функционал редактирования организаций

## Обзор

Добавлена полноценная возможность редактирования юридических лиц и холдингов в компоненте OrganizationSelector.

## Новые возможности

### 1. Редактирование юридического лица

**Кнопка редактирования:**
- Синяя иконка карандаша (Edit) рядом с каждым юр. лицом
- При клике открывается модальное окно с формой редактирования
- Форма предзаполнена текущими данными

**Редактируемые поля:**
- Название (русский) - `name`
- Название (английский) - `nameEn`
- Страна - `country` (KZ, RU, GE, AM, EU)
- Тип - `type` (LLC, IP, Branch, Holding)
- ИИН/БИН - `taxId`
- Холдинг - `holding` (опционально)

**Функциональность:**
```typescript
handleEditEntity(id: string)
  - Находит юр. лицо по ID
  - Заполняет форму текущими значениями
  - Открывает модальное окно

handleUpdateEntity()
  - Валидирует обязательные поля
  - Вызывает updateLegalEntity из контекста
  - Показывает уведомление об успехе
  - Сбрасывает форму
```

### 2. Редактирование холдинга

**Кнопка редактирования:**
- Синяя иконка карандаша (Edit) рядом с каждым холдингом
- При клике открывается модальное окно с формой редактирования
- Форма предзаполнена текущими данными

**Редактируемые поля:**
- Название холдинга (русский) - `name`
- Название холдинга (английский) - `nameEn`
- Список компаний - `entities[]` (с чекбоксами)

**Функциональность:**
```typescript
handleEditHolding(id: string)
  - Находит холдинг по ID
  - Заполняет форму текущими значениями
  - Предвыбирает связанные компании
  - Открывает модальное окно

handleUpdateHolding()
  - Валидирует обязательные поля
  - Вызывает updateHolding из контекста
  - Показывает уведомление об успехе
  - Сбрасывает форму
```

## UI/UX улучшения

### Кнопки действий

Каждое юр. лицо и холдинг теперь имеет набор кнопок:
- ✓ **Check** (зелёная галочка) - отображается для выбранного элемента
- ✏️ **Edit** (синий карандаш) - открывает модальное окно редактирования
- 🗑️ **Delete** (красная корзина) - удаляет элемент после подтверждения

### Модальные окна

**Заголовки:**
- При добавлении: "Добавить юр. лицо" / "Add Legal Entity"
- При редактировании: "Редактировать юр. лицо" / "Edit Legal Entity"

**Кнопки:**
- При добавлении: "Добавить" / "Add"
- При редактировании: "Сохранить" / "Save"
- Кнопка "Отмена" / "Cancel" сбрасывает форму и закрывает окно

### Предотвращение всплытия событий

Кнопки Edit и Delete используют `e.stopPropagation()`, чтобы предотвратить:
- Выбор организации при клике на кнопку действия
- Случайное переключение между режимами просмотра

## Интеграция с OrganizationContext

Использует существующие функции из контекста:
- `updateLegalEntity(id, updates)` - обновление юр. лица
- `updateHolding(id, updates)` - обновление холдинга

Эти функции автоматически:
- Обновляют state в контексте
- Сохраняют данные
- Уведомляют подписчиков об изменениях

## Мультиязычность

Все тексты поддерживают переключение языка:
- Русский (`language === 'ru'`)
- Английский (`language === 'en'`)

Переводятся:
- Заголовки модальных окон
- Названия полей форм
- Текст кнопок
- Уведомления toast

## Валидация

**Обязательные поля для юр. лица:**
- Название (русский)
- Название (английский)

**Обязательные поля для холдинга:**
- Название холдинга (русский)
- Название холдинга (английский)

При отсутствии обязательных полей:
- Показывается ошибка toast
- Сохранение не происходит
- Форма остаётся открытой

## Примеры использования

### Редактирование юр. лица
```typescript
1. Пользователь кликает на иконку карандаша у "ТОО ФинТех Казахстан"
2. Открывается модальное окно с заполненными полями
3. Пользователь меняет название на "ТОО ФинТех KZ"
4. Нажимает "Сохранить"
5. Показывается уведомление "Юр. лицо обновлено"
6. Модальное окно закрывается
7. Список обновляется с новым названием
```

### Редактирование холдинга
```typescript
1. Пользователь кликает на иконку карандаша у "Холдинг Евразия"
2. Открывается модальное окно с выбранными компаниями
3. Пользователь добавляет новую компанию в холдинг
4. Нажимает "Сохранить"
5. Показывается уведомление "Холдинг обновлен"
6. Модальное окно закрывается
7. Холдинг теперь включает новую компанию
```

## Технические детали

### State Management

Используются состояния:
```typescript
const [editingEntity, setEditingEntity] = useState<string | null>(null);
const [editingHolding, setEditingHolding] = useState<string | null>(null);
```

При редактировании:
- `editingEntity` содержит ID редактируемого юр. лица
- `editingHolding` содержит ID редактируемого холдинга
- Форма переключается в режим редактирования

### Сброс формы

При закрытии модального окна или успешном сохранении:
```typescript
setNewEntityName('');
setNewEntityNameEn('');
setNewEntityCountry('KZ');
setNewEntityType('LLC');
setNewEntityTaxId('');
setNewEntityHolding('');
setEditingEntity(null);
```

## Совместимость

Функция работает с:
- ✅ Компактным режимом (`compact={true}`)
- ✅ Полным режимом
- ✅ Всеми существующими функциями OrganizationContext
- ✅ Фильтрацией данных на других страницах
- ✅ Мультивалютным отображением

## Безопасность

- Подтверждение удаления через `window.confirm()`
- Валидация обязательных полей
- Сброс состояния при закрытии модалов
- Предотвращение случайных кликов через stopPropagation

## Будущие улучшения

Потенциальные доработки:
- [ ] Drag & drop для изменения порядка организаций
- [ ] Массовое редактирование
- [ ] История изменений
- [ ] Возможность дублирования организации
- [ ] Импорт/экспорт организаций
- [ ] Расширенная валидация (формат ИНН, уникальность и т.д.)
