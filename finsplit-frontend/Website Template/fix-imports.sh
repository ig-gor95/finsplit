#!/bin/bash

# Скрипт для исправления импортов с версиями

cd "$(dirname "$0")"

echo "🔧 Исправление импортов..."

# Исправить все импорты @radix-ui с версиями
find src/components/ui -name "*.tsx" -type f -exec sed -i '' 's/@radix-ui\/\([^@]*\)@[0-9.]*/@radix-ui\/\1/g' {} +

# Исправить импорты next-themes с версией
find src -name "*.tsx" -type f -exec sed -i '' 's/next-themes@[0-9.]*/next-themes/g' {} +

echo "✅ Готово! Все импорты исправлены."
echo ""
echo "Теперь запустите: npm run dev"

